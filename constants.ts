
// Feature flags
export const FLAGS = {
  forceHybridWhenLedgerPresent: true,
};

// The ethical cap for the HAS score in hybrid mode
export const ETHICAL_CAP = 75;

// The "glass ceiling" cap for artifact-only mode is now raised to match the ethical cap.
export const ARTIFACT_ONLY_CAP = 75;

// The policy model version
export const PPM_MODEL_POLICY = 'PPM-HAS v0.5-consenso';